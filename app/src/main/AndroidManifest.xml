<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sprinkles">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sprinkles">
        <activity android:name=".DetailActivity"></activity>
        <activity android:name=".OrderActivity" />
        <activity android:name=".Food5Activity" />
        <activity android:name=".Food4Activity" />
        <activity android:name=".Food3Activity" />
        <activity android:name=".Food2Activity" />
        <activity android:name=".FoodActivity" />
        <activity android:name=".CategoryActivity" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".SignUp_Activity" />
        <activity android:name=".MainActivity2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Models.OrderClass" />
    </application>

</manifest>